import { PartenairesFilterPipe } from './partenaires-filter.pipe';

describe('PartenairesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartenairesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
